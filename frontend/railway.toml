# Frontend — Railway
# Root Directory OBLIGATORIO: frontend
# NO uses frontend/Dockerfile (ese es solo para docker compose local)
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.railway"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
