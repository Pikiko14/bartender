# Backend — servicio con Root Directory vacío (raíz del repo).
# Frontend: crea OTRO servicio con Root Directory = frontend
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
